node_modules/
*.log
.tmp/
.playwright-mcp/
# 临时调试脚本不入库
server.cjs
.ci-check.js
test-cases/*.tmp

# 本机 AI 工具链配置（各 AI 助手的 skills/缓存，非项目产物）
.agents/
.claude/
.cline/
.codebuddy/
.codegraph/
.continue/
.freebuff/
.junie/
.kiro/
.ignore
.mcp.json
AGENTS.md
opencode.json
skills-lock.json
# 临时验证脚本（静态语法/夹具核验用，不入库）
verify-*.cjs
!verify-acceptance.cjs
# 临时真机测验证页（不入库）
verify-kokoro.html

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
# Tauri Rust 构建缓存（2.1G 级，绝不可入库；重新 `npx tauri build` 即再生）
/src-tauri/target/

# 桌面版构建产物（安装包/免安装 exe，体积大，按需手动处理，不入库）
/发布/

# 本机归档（中间产物/旧版变更报告副本）
/归档/
