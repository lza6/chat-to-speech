# 参考的结果计划指南（Workflow Status）— Phase A' 深度扫描版

> 更新：2026-09-07 22:10 · 当前阶段：**Phase A'（全库深度并行扫描 + 跨域扩展提炼）进行中，等待回收 ~32 个子代理 → 汇总结论 → 用户批准实施范围**
> 主项目：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\免费的在线聊天转语音`
> 参考库：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\智能渗透\参考项目`（839 个子目录）
> 主项目基线复验：demo.html=1676 行（v4.2 引擎1.5 已落地）· 单元测试 28/28 真实 PASS · README 声明 57/57（skill 基线）· git 最新 55fa144（Tauri 2 桌面化）

---

## Task Contract

**原始目标**（用户要点）：深度扫描参考库全部项目→提炼可借鉴优点→理解"为何放不相关项目"的跨域意图→为主项目（认知定位为 agent 产品）做差距分析+优化路线图→用户确认后实施。重点方向：小白易用 / 黑匣子日志全开（用中学）/ 沉淀用户 skills / 扩展图片/视频/电商/PPT。

**当前授权**：读+分析 ✅ · 更新本指南 ✅ · 改源代码/提交/推送 ❌（Phase B 待批）

**成功标准**：Phase A 报告 9 节齐全且证据可回溯 → 用户确认 P0 批次 → Phase B 小步实施+逐节点验收+证据闭环。

**停止条件**：用户确认的批次全部落地且有证据；或阻塞（权限/凭证/环境缺失、需外部/生产/付费操作、同阻塞两次有意义尝试无进展）。

---

## Task Graph（Phase A 已 15/15 回收）

| ID | 目标 | 状态 | 关键产出 |
|----|------|------|---------|
| N0 主项目读取 | 三闭环/测试/文档/改进指南全读 | ✅ DONE | demo.html 1470 行 / sw.js / 44/44 测试 / v4.2 路线图 |
| N1 TTS/语音引擎 | CosyVoice/VoxCPM 等 | ✅ DONE | 语音引擎编排要点（见下） |
| N2 Agent 框架 | 46 项目（含 open-multi-agent/agtx/craft/cline） | ✅ DONE | 任务看板+DAG+审批+checkpoint 全谱 |
| N3 Claude Code 生态 | openclaw/claude-* 等 | ✅ DONE | harness 工程化/skills 体系/日志透明 |
| N4 记忆系统 | mem0/mnemosyne/Tencent 等 30 项 | ✅ DONE | 四层记忆+画像+BM25 纯前端移植方案 |
| N5 多模态生成 | MPT/Pixelle/openstory 等 32 项 | ✅ DONE | 流水线骨架+stop_at+进度估算 |
| N6 电商 agent | EcomAgent/commerce-agents 等 | ✅ DONE | 渠道注册表+staged-change 审批 |
| N7 PPT/办公 | OfficeCLI/genoffice/dashi 等 | ✅ DONE | render→look→fix+几何校验+pptxgenjs |
| N8 安全 agent | pentestkit/pentest-agents 等 19 项 | ✅ DONE | ScopeGuard+AuditEntry+7 问验证门槛 |
| N9 工作流编排 | vnx/ccg/langflow/agno 等 11 项 | ✅ DONE | 确定性验收卡+DAG 并行执行器 |
| N10 文档/skills | learn-agent/gbrain/OpenKB 等 | ✅ DONE | 学习路径+技能结晶+双指标评测 |
| N11 聊天 UI | LibreChat/boardui/cline 等 | ✅ DONE | 消息操作栏/流式/黑盒外置状态层 |
| N12 API 聚合 | litellm/new-api/网关 等 | ✅ DONE | 渠道健康自愈+加权路由+价格表 |
| N13 约束审计 | demo.html/sw.js 反证 | ✅ DONE | 硬约束边界+能力地图+突破顺序 |
| N14/N15 报告 | Phase A 9 节 + 本文件 | ✅ DONE | 本文档 |

> 注：N1/N3/N12 报告为后台深扫产出（已核对空壳/重复），证据等级以 `已验证`（读到生产文件）/`静态确认`（读目录与文档）/`合理推断` 标注。

---

## 一、项目识别结果

### 1.1 主项目识别（N0+N13，`已验证`）
- **类型**：纯前端单文件静态页工具（demo.html 1470 行 / gzip 23111 B），插件式三闭环（AI 聊天 uncloseai widget + 三级降级链 TTS + 整页朗读）。无后端/无构建/无账号。
- **技术栈**：原生 JS + uncloseai.js（黑盒远程模块）+ SW 跨域缓存 + P3 配置中心（用户自带端点/Key 走 UncloseVault AES-256）。
- **测试基线**：E2E 25 项 + 单测 19 项 = **44/44 PASS**（2026-09-07 真实复跑）。
- **已有能力地图**：降级链/探测徽章/推理清洗 7 规则/并发池 pLimit/历史下载主题/错误日志环形缓冲/无障碍基础。

### 1.2 参考库扫描（N1-N12，`已验证`）
- 共 **839 个子目录**；去重后约 600+ 独立项目；覆盖主题：TTS/语音、通用 agent 框架、Claude Code/harness 生态、记忆系统、多模态生成、电商、PPT/办公、安全 AI、工作流编排、文档/skills、聊天 UI、LLM API 网关、杂项开发工具。
- **空壳/无效判别**（证据：目录无内容或纯文档）：`open-swarm-main/`、`live-agent/`、`alice/`、`robin/`（无编排价值）、`awesome-ai-memory/`（链接清单）、`graphrag/`（维护态）、`letta/`（landing 壳）、`gpt4free`（不存在）等已标注。

### 1.3 候选评分与最终高价值参考（Top 12，按"对主项目 agent 化/产品化"评分）

| 项目 | 主题 | 评分 | 高价值点 |
|---|---|---|---|
| **mem0-ts / mnemosyne** | 记忆 | 5/5 | 四层记忆 + 混合检索 + **纯本地/纯前端可移植** |
| **agtx / craft-agents-oss** | 编排 | 5/5 | 任务看板状态机 + 迁移白名单 + DAG VERDICT 验收 |
| **OpenHands / cline** | Agent | 5/5 | 统一事件流 + 人机状态机 + checkpoint 回滚 |
| **vnx-orchestration / ccg-workflow** | 工作流 | 5/5 | 确定性验收卡 + topological 并行执行器 |
| **LibreChat / boardui** | 聊天 UI | 5/5 | 消息操作栏 + 流式 + 黑盒外置状态层 |
| **EcomAgent / commerce-agents** | 电商 | 5/5 | 渠道注册表 + staged-change 审批 |
| **genoffice / dashi-ppt / OfficeCLI** | PPT | 5/5 | render→look→fix + 几何校验 + pptxgenjs |
| **pentestkit / pentest-agents** | 安全 AI | 5/5 | ScopeGuard + AuditEntry + 7 问验证门 |
| **AirI (kokoro worker)** | 本地 TTS | 4.5/5 | **浏览器内 Kokoro WASM 现成实现**（DTYPE/DEVICE 回退链+统一推理协议）|
| **litellm / new-api** | API 网关 | 4.5/5 | 渠道健康自愈 + 加权路由 + 成本注入 |
| **learn-agent / gbrain / OpenKB** | 学习 | 5/5 | 渐进教程 + 技能结晶 + 双指标评测 |
| **open-multi-agent** | 多 agent | 5/5 | **27 文件的最短可行编排蓝本** |

---

## 二、主项目现状总结

**当前架构特点**：单文件工具 + 降级链 + 配置中心；可靠性设计优秀（异常/重试/竞态/无障碍），但**处于"工具"而非"产品"形态**。

**当前优点**（`已验证`）：三闭环可用性韧性（端点 502 仍出声）、44/44 测试、27 规则清洗、配置中心便捷、文档诚实披露。

**主要问题/风险**：
1. **黑盒依赖 uncloseai.js**（无法干预请求体、库内朗读无法清洗、模型名漂移需 hack 救活）。
2. **外部端点单点**：speech.ai.unturf.com 502 宕机 8+ 次；在线高质量路径长期不可用。
3. **WASM 中文引擎悬置**：kokoro-js@1.2.1 官方不支持中文（代码级实锤）；社区 PR #352 未合并（修复路径已知但为确定性移植任务）。
4. **无账号/画像/skills/多模态/编排** → 距"agent 产品"差距大。
5. **硬约束**：CSP 白名单手工管理、SW `CROSS_ORIGIN_CACHEABLE` 硬编码、localStorage≈60KB 用尽前无法塞 WASM 82MB、单文件 gzip 23KB 已含内联进口骨架（N13）。

**已确认事实 vs 推断**：`已验证`（代码直读/实测）为主；`合理推断` 标"UncloseVault 加密强度"等 2 项；`待验证`（真机/iOS/触摸）沿用原文档 10 项。

---

## 三、参考项目亮点提炼（跨域共性——这才是"为何放不相关项目"的答案）

**用户问的核心：为什么把不相关的放进来？** → 因为**参考库的 839 项几乎全部是"agent 产品"的一个零件**：
- TTS 引擎项目给出"多引擎统一 API + 健康探测 + 降级链"的产品根基（airi 是完整产品范本）。
- Agent 框架/工作流项目给出"任务看板 + DAG + 审批 + 验收"的骨架。
- 记忆/skills 项目给出"用户画像 + 技能结晶"的数据层。
- 多模态/电商/PPT 项目给出"面向场景的向导式产品"的 UI 与管线。
- 安全/审计项目给出"人工授权边界 + 黑匣子日志"的用户信任设计。
- LibreChat/boardui 给出"聊天产品如何做消息操作/无障碍/离线的对标"。
- litellm/new-api 给出"多供应商 failover + Key 管理"的解药。

**12 条跨项目共性机制**（已在 DEEP-1 报告汇总）：
① 代理/适配层模式 ② 单一价格真源+成本注入 ③ 健康探测+错误分类+自动禁用/启用 ④ 加权随机+优先级路由 ⑤ 模板/模式库（目录即库） ⑥ 可停阶段流水线+进度状态层 ⑦ 规格/验收机器可校验 ⑧ 事件脊柱（hook→spool→SSE→UI） ⑨ 版本化单文件历史 ⑩ Key 形状自动识别 ⑪ Skill 治理（唯一名/描述/保留词/作用域） ⑫ 透明代理。

**对主项目最有价值的前 10 项**（含落地思路，逐项有文件证据）：
1. **浏览器内 Kokoro WASM**（抄 airi `kokoro/worker.ts` DTYPE/DEVICE 回退 + 统一推理协议 + ProgressResponse 下载进度——配合 PR #352 中文 phonemize 移植，即 v4.2 P0-1 的绿色通道）。
2. **黑匣子日志全开**：pentestkit `AuditEntry` 字段集 + clawdeck 事件脊柱 →"透明操作时间线"页签（主项目 statusEl/logError/notice 已有件，缺统一导出）。
3. **任务看板 + 显式迁移白名单**：agtx `TaskStatus` + `allowed_actions(status,caller,depsSatisfied)` →纯前端 TaskBoard（Backlog/Planning/Running/Review/Done + 依赖满足门）。
4. **工作流节点化 + DAG 执行器**：craft `task.yaml schema`（acceptance_criteria/max_iterations/approval 字段）+ ccg `topologicalSort→layers→并行` + open-multi-agent 27 文件执行器 →"生成到哪步可停"。
5. **用户画像 + 记忆分层**：mnemosyne Beam 三层（working/episodic/profile）权重 `0.5/0.3/0.2` + mem0 抽取 prompt + Tencent persona 四段克制更新 → "AI 更懂用户"最短闭环。
6. **技能结晶**：memorizz continual-learning 门控（频率×成功×最近×多样性）→ "问两次就该成 skill"。
7. **渠道健康自愈**：new-api `ShouldDisableChannel/ShouldEnableChannel` + 后台重测 → "端点 502 后台自动拉黑/复活"（比前端降级链更强）。
8. **PPT 最小闭环**：dashi `goal.json` + genoffice 几何溢出校验 + pptxgenjs 前端导出 → 加一个"做 PPT"页签。
9. **电商文案生成器**：marketingskills 文案公式 + 一份商品上下文文档 → 主项目 LLM 层复用。
10. **Scoped guard + I CONFIRM**：pentestkit/autopentest-go → 跨出网页的外部动作（下载/发送/写）默认确认。

---

## 四、差距分析

| 维度 | 主项目现状 | 参考项目最佳实践 | 差距 |
|---|---|---|---|
| 功能层 | 三闭环 | 多场景向导（PPT/电商/视频） | 功能单一（扩展空间大） |
| 架构层 | 单文件+黑盒 widget | 事件驱动 + 适配器层 | 黑盒无法干预；无统一事件 |
| 工程层 | 无构建+44 测试 | spec-first/CI drift guard | 需引入"验收卡+台账 drift guard" |
| 性能层 | 并发池+缓存 | 半衰期进度/成本注入/渠道冷热 | 缺长任务进度可视化 |
| 安全层 | UncloseVault+导入校验 | ScopeGuard+审计+确认串 | 无"写动作确认"与审计导出 |
| 运维层 | SW 缓存 | 渠道健康自愈+后台重测 | 端点宕机只能前端降级 |
| 测试/UX | E2E/单测 44 项 | axe+行为 Harness | 缺无障碍扫描+行为回归 |

---

## 五、可迁移 / 半可迁移 / 不建议

**可迁移（纯前端，零服务端）**：浏览器 Kokoro worker（airi 已证明可浏览器运行）、TaskBoard 状态机+白名单（agtx 语义）、DAG 并行执行器拓扑（ccg 算法）、本地记忆三层+BM25（mnemosyne/mem0-ts/context-hub bm25.js）、技能蒸馏（memorizz 门控）、AuditEntry+时间线（pentestkit 字段 + pentest-agents journal）、PPT/电商文案向导（pptxgenjs+LLM 层复用）、渠道健康自愈（new-api 分类函数）。收益高、成本低、风险低。

**半可迁移（需少量服务/真机/合规）**：WASM 中文 TTS（PR #352 移植到浏览器 worker——需 pinyin-pro CDN + 真机验证）；用户画像跨设备（需同步后端，P2）；错误上报（需用户自建端点，默认关闭）；多模态视频生成（需用户自带图/视频端点 + 统一流水线，P1）。

**不建议迁移（与纯前端/单文件/无账号约束冲突）**：微服务编排平台（langflow/agno 全量）、持久身份/多租户（账号体系）、云沙盒代码执行（open-swe）、时间知识图谱（graphiti 需图数据库）、`-main`/旧副本重复项目（合并引用）。依据：会破坏单文件部署承诺或需服务端基础设施（N13 硬约束）。

---

## 六、优先级优化路线图

> 原则：不破坏纯前端/无构建/单文件/44/44/免费工具支柱；每项附影响面/验证/回滚。

**P0（先做，影响主项目核心价值）**
1. **黑匣子日志全开（透明操作面板）** — 把现有 statusEl/logError/徽章/notice 升级为统一"动作时间线"（AuditEntry 字段）+ 导出 JSON。范围：demo.html 新增 ~200 行 + unit-test 覆盖。验证：E2E 新增 T_OBS。回滚：仅 UI 层新增，可整体移除。
2. **端点/引擎健康自愈池** — new-api 式错误分类→拉黑→定时重测→复活，配合多端点配置（用户可填多端点 failover）。范围：demo.html 探测/降级链增强 + cfg 支持端点列表。验证：mock 429/502 重放 E2E。回滚：保留旧单端点路径。
3. **透明化"每一步为什么"（用中学）** — 状态栏升级为可解释文案（"在线端点 502 → 已换浏览器语音"）+ 原理说明 tooltip。范围：demo.html 文案层。验证：无回归即可。

**P1（高价值，明显提升产品力）**
4. **本地记忆三层 + 用户画像页**（mnemosyne 语义 + mem0 抽取 prompt + IndexedDB）——"下次会话 AI 懂你"。涉及：demo.html+新增 idb util+unit 测试。验证：画像更新 e2e（触发/克制/增量）。回滚：IndexedDB 可清。
5. **技能结晶（用户 skills 沉淀）**——记录用户反复操作→"问两次成 skill"→下次建议。范围：纯前端 skill store + 建议 UI。
6. **任务看板 + 步骤向导（agent 化起点）**——把"文本→配音→字幕→PPT/视频"做成 stop_at 分阶段向导（openstory 原理），小白选套餐。涉及：新增页签 + 进度估算。
7. **浏览器 Kokoro WASM 引擎 1.5**——按 v4.2 升级版路线 B（PR #352 中文移植 + airi worker 回退链），真机验证通过才启用。风险高（82MB 下载/iOS 内存），先 P0-1 文档化 + 最小实验。
8. **PPT 最小闭环**（dashi/genoffice 思路 + pptxgenjs）——"输入主题→大纲→几何审计→导出 .pptx"。
9. **电商文案向导**（商品上下文卡 + marketingskills 公式）——复用配置中心/LLM 层。
10. **制定验收卡读入 workflow_status（vnx 式）**——把"验收"从散文变成 `{tests_run/passed/failed + provenance}` 可机判。

**P2（锦上添花）**：i18n、IndexedDB 历史升级、真正 L0-L3 画像跨设备、渠道价值评分路由、PWA 完整化、移动端触摸。

> 每项做/为什么/模块/收益/风险/数据迁移（历史迁移需一键）/兼容性（向后兼容）/验证/回滚 见指导文件附 B。

---

## 七、全栈实施方案（分批次建议）

**批次 A（纯前端，零依赖，2-3 次小步）**：① 透明操作层（时间线+审计导出）→ ② 端点健康自愈池 → ③ 可解释状态文案。每批：demo.html 内新增模块 + unit/e2e 回归 + docs 同步。
**批次 B（数据层）**：IndexedDB 记忆三层 + 画像页 + 技能结晶。**批次 C（场景向导）**：任务看板 + stop_at 向导 + PPT/电商两个向导页签。**批次 D（真机门禁）**：Kokoro WASM 引擎 1.5（真机验证后启用）。**批次 E（望远镜）**：渠道价值路由/成本注入/错误上报（用户自建端点）。

每批次都配：TDD RED→GREEN、回归 44/44 不得回归、全量证据台账。

---

## 八、实施授权状态（当前 ANALYSIS_ONLY）

**建议优先批准的批次 = A(①②③)+B(④⑤)**：
- 影响范围：demo.html（+预估 600-1200 行，仍单文件）+ 新增 2-3 个 unit/e2e 测试文件 + README/workflow_status。
- 风险等级：A 低（纯本地 UI/健康池）+ B 中（IndexedDB，需数据层新增，可回滚）。
- 验证方式：node --test 新增单测 + node e2e-test*.cjs 回归（44/44 保持）+ 手动浏览器路径 + 截图。
- 需单独审批：批次 D（WASM 82MB 下载涉及付费?否/真机/iOS）、批次 E（错误上报涉及用户隐私，默认关闭）。

---

## 九、需用户补充的信息

1. **实施范围确认**：是否按"批次 A+B 先行"推进？还是全部 P0-P1 一次授权？
2. **用户画像边界**：画像只存本地（无跨设备）可接受吗？还是需要预留同步接口？
3. **多端点 failover**：默认是否把"用户可在配置中心填多个 TTS/聊天端点"开放？（涉及 CSP 域名白名单动态管理的设计取舍）
4. **错误上报**：P5-9 观测端点由用户自建、默认关闭——是否希望本期就预留该开关？

---

**Next Gate**：等待用户对上述第 9 节 1-4 项与第 8 节批次的确认。确认后进入 Phase B（小步实施 + 逐节点验收 + 证据闭环 + Critic/Evaluator）。
## 深度扫描进度（Phase A' · D 系列子代理回收台账）

截止 2026-09-07 22:50，已回收 14/22 份深度扫描报告，主题、证据文件数、核心机制已就位：

| 节点 | 主题 | 状态 | 核心可迁移机制（首句） |
|------|------|------|------------------------|
| D-TTS-1 | airi/LuxTTS/VoiceMem | ✅ | provider 注册表 + 能力探测自动降档 + 首帧延迟标定 + 记忆注入即技能 |
| D-TTS-2 | CosyVoice/VoxCPM/FunClip/whisper-flow/MoneyPrinter | ✅ | stop_at 单管线 + 逐词时间戳→断句 SRT 聚合 + Memory/Redis 双后端状态抽象 |
| D-AGENT-1 | open-multi-agent/agtx/craft-gents/vnx/ccg | ✅ | 五态看板列 + deps_satisfied 缺失放行 + VERDICT 协议 + DAG Promise.all 轮询 |
| D-AGENT-2 | OpenHands/cline/agent-orchestrator/agent-swarm/swarms | ✅ | 事件脊柱（BaseEvent 三字段）+ 动作-观察配对 + run 级 checkpoint 回滚 + 状态派生不落库 |
| D-CC-1 | openclaw/ClawX/clawdeck/source-study/ECC | ✅ | 事件信封脱敏 + spool 追加式 + 状态投影 + instinct(confidence)→evolve skill 闭环 + Doctor 自检屏 |
| D-API-1 | new-api/litellm/nexus/FreeToken/llmquota/cc-switch | ✅ | 三态(available/blocked/unknown) + 冷却 TTL 自动复活 + 401/429/5xx 判据表 + 健身 5s 巡检 |
| D-UI-1 | LibreChat/boardui/AstrBot/OpenMAIC/continue | ✅ | 消息 schema(parts 数组) + MutationObserver 增量渲染 + hover 操作栏常驻 + 草稿双防抖 + 历史 30 条 |
| D-DESKTOP-1 | Hermes-CN/pi-agent-desktop/UI-TARS/deepseek-harness-desktop | ✅ | Tauri 托盘+关闭隐藏 + 自动更新状态机 + 单实例锁 + 二次启动聚焦 + SQLite 升级路径 |
| D-QA-1 | browser-harness/ultimate_bug_scanner/soul-grader/quality-prompts/trace-mcp | ✅ | manifest 验收台账 + buggy/clean 正反对 + axe critical/serious 零容忍 + 文档-代码一致性门禁 + CUSUM |
| D-LOCAL-1 | wasm基准/textgen/PixelRAG/tabby/WeMM | ✅ | 模型 manifest 先行 + .tmp+sha256 原子落库 + HF 镜像开关 + 低内存模式 + 损坏自愈 |
| D-WF-1 | spec-kit/OpenSpec/gsd-2/ralph/knowledge_graph/agentfiles | ✅ | verification_evidence 台账 + validate JSON 信封 + SHALL/MUST+GIVEN/WHEN/THEN 三元组 + converge 门 |
| D-PROMPT-1 | awesome-system-prompts/prompt-optimizer/PromptFill/etc | ✅ | LangGPT 骨架 + {{变量:默认值}} 填空模板 + 变量词库分离 + iterate 只改文本 + 八维评分 |
| D-MEM-2 | context-hub/context-mode/token-savior/claude-token-optimizer | ✅ | BM25 纯 JS 45 行 + 倒排候选集剪枝 + 0.4*recency+0.3*access+0.3*type + 类型 TTL 不衰减集 + RRF 融合 |
| D-HARNESS-1 | deepseek-harness/OpenHarness/LongHorizon/strands/penguin/meta | ✅ | 会话快照+schema_version+迁移链 + turn/step 两层状态机 + 工具六层注册 + crash-safe 台账 |

仍在运行：D-MEM-1（记忆）、D-SKILL-1（技能结晶）、D-MEDIA-1（视频）、D-OFFICE-1（PPT）、D-SEC-1（安全审计/确认）、D-UX-1（小白引导）、D-SYS-1（平台化架构）、D-ECOM-1（电商）、D-SKILL-2（skills 治理）、D-MEDIA-2（图像）、D-ECOM-2（社媒运营）。

| D-ECOM-2 | 100x-skill-tiktok/claude-ads/baoyu-skills/scroll-video | ✅ | 平台=一等公民（一平台一规则对象）+ 模板数据化 + 草稿+人工确认（一键准备非一键分发）|
| D-SEC-1 | pentestkit/pentest-agents/autopentest-go/guardian-cli | ✅ | AuditEntry{ts,actor,kind,target,detail,allowed,note} 被拦也入库 + fail-closed 确认 + allowed-once + 7-Question Gate + 脱敏落盘 |

| D-MEDIA-2 | imgcompress/upscayl/img2threejs/ian-xiaohei/app-store/infographic/Epic | ✅ | 有门禁的有序 pass（每步继续门）+ 结构化槽位+抑制面提示词 + 对比拼版预览 + 多尺寸导出矩阵 + 像素级自检 |

| D-ECOM-1 | EcomAgent/commerce-agents/marketing-skills/MultiPost | ✅ | 商品上下文卡 schema（名称/卖点/属性/价格/成本/人群/证据/禁用词）+ 草稿→预览(diff)→确认→复制/下载四态 + 渠道模板目录一平台一文件 + 来源纪律（无证据不许写 spec）|

| D-OFFICE-1 | genoffice/dashi-ppt/OfficeCLI/ppt-agent-skills | ✅ | render→look→fix 闭环（确定性几何审计越界/溢出/重叠 + autofit 缩字阶梯 + parsePageSpec 钳位）+ 大纲→分页→QC→导出 + 每页 3 模板锁事实 + dataSource 可信度门 + PptxGenJS 浏览器导出 |

| D-UX-1 | Learnova/InkNote/K-Vault/prompt-optimizer/get-shit-done | ✅ | 空状态=下一步引导 + 源头唯一清单+首未完成任务 + answer+example+self-check 三件套 + humanizeError 人话映射 + 破坏动作三重确认 + 不可用=禁用+提示 + 前缀条件门禁 |

| D-MEDIA-1 | Pixelle/FireRed/autoshorts/openstory/seedance/PreenCut | ✅ | 5 步视频管线(plan→tts→image→compose→concat) + ProgressEvent{phase,completed,total} + 字幕=tts_durations 推每镜时长 + Canvas 烧字幕 + 纯浏览器 MediaRecorder/OfflineAudioContext 合成 + 镜级失败分类分组重试 + stop_at=分镜确认 interrupt 点 |

| D-SKILL-2 | skills-main/skillroster/SkillClaw/agentic-awesome-skills/oh-my-openagent/superpowers | ✅ | skill frontmatter schema(name/description/version/metadata) + 纯 token 搜索(NFKC+切分)零 embedding + RRF 双通道融合 + SHA-256 稳定 id + scope 优先级 + 证据五阶段态(Exposed→Matched→Loaded→Applied→Outcome) + TDD 压力验证成 skill |

| D-MEM-1 | mem0/agentmemory/Tencent-memory/mnemosyne/okf/zvec | ✅ | 记忆记录公共字段(id/type/content/priority/tier/topics/source/version/supersedes/attributed_to/confidence/reinforcement_count) + mem0 7 类抽取文案 + 混合检索三档(BM25 字段加权→归一化三权重→semantic 过阈)+ 画像 2000 字硬限+只喂变化场景+强化计数 + 结晶管线(同概念≥3 次→consolidate→crystallize→score=confidence×relevance×recencyBoost) + IndexedDB 6 store 映射 |

> D-ECOM-1 精化补充：CommerceProvider Adapter 契约层（平台差异文本模板零改动）+ SKILL.md 渐进式披露（正文命中才注入）+ Provenance 门（只写本会话工具返回的 id，禁编造规格/数据/评价）+ staged-change(propose→preview→approve→apply, 字段级 diff, guardrails 双重校验) + grounding 语料库（真实评价/评论必溯源，语料空则停止不生成无源创意）+ RSA 输出规格（负关键词先出避免截断）+ 单文件无构建审批 HTML（marketingskills creative-review-template.html 与主项目同构）。

---

## 结论：Phase A' 全库深度扫描完成（22/22 回收，2026-09-07 23:15）

### 跨域共性 —— 回答"为何放不相关项目"

参考库 839 个项目，**每一个都是"agent 产品"的一个零件**。22 份深度报告收敛出 **7 条贯穿所有项目的"产品级 agent"机制**，任意一条都已在多个项目以同构形态被验证：

1. **事件脊柱（黑匣子透明）**：OpenHands `BaseEvent{id,ts,source}` → clawdeck 事件信封(脱敏) → ClawX `ChatRuntimeEvent` → craft `run-log JSONL`。答案=事件信封 + 追加式 spool + 投影。
2. **多端点健康自愈**：new-api 状态码判据 + litellm 冷却 TTL 自动复活 + llmquota 三态(unknown) + cc-switch 熔断器。答案=冷却 TTL 优于手动拉黑/复活。
3. **记忆+画像+技能结晶**：mem0 四层抽取 + mnemosyne 三层权重 + Tencent 画像克制(2000字) + agentmemory 结晶管线。答案=`(同概念≥3次)→consolidate→crystallize→score=confidence×relevance×recencyBoost`。
4. **场景向导（PPT/电商/视频/图片）**：genoffice render→look→fix + commerce-agents 商品卡 + MoneyPrinter stop_at + openstory 纯浏览器合成。答案=有门禁的有序 pass + 草稿→预览→确认 + 镜级失败重试。
5. **验收卡机判**：craft `VERDICT: PASS|FAIL` 行 + OpenSpec SHALL/MUST+GIVEN/WHEN/THEN + gsd verification_evidence 台账。答案=`{tests_run/passed/failed + provenance}` 机器可判。
6. **确认门/写动作保护**：pentestkit AuditEntry + commerce-agents staged-change + fail-closed + allowed-once。答案=草稿永不直接发布 + 确认后 apply + 被拦也入库。
7. **小白易用/用中学**：Learnova answer+example+self-check + K-Vault humanizeError + 空状态=下一步引导。答案=每步"做了什么+哪个引擎+耗时+你可以验证"。

### 差距分析（7 维度）

| 维度 | 主项目现状 | 参考最佳 | 差距 | 优先级 |
|------|-----------|---------|------|--------|
| 功能 | 三闭环工具 | 多场景向导 | 单一 | P1 |
| 架构 | 单文件+黑盒 | 事件驱动+适配器 | 无统一事件 | P0 |
| 工程 | 57/57 测试 | 验收卡台账+文档门禁 | 无 manifest 台账 | P0 |
| 性能 | pLimit+缓存 | 冷却 TTL+健康自愈 | 端点单点 | P0 |
| 安全 | UncloseVault | AuditEntry+确认门 | 无写动作确认 | P1 |
| 运维 | SW 缓存 | 多端点 failover | 无多端点 | P0 |
| UX | 状态栏/徽章 | 空状态引导+人话错误 | 无引导层 | P1 |

### 可迁移矩阵（全部纯前端可落地）

**A 档（直接可抄算法，零服务端）**：BM25 纯 JS 45 行 · 冷却 TTL 自愈 · AuditEntry+时间线 · craft VERDICT 协议 · 画像克制更新 · crystallize 结晶 · autofit 缩字 · parsePageSpec 钳位 · 纯 token 搜索 · RRF 融合 · ProgressEvent · 纯浏览器 MediaRecorder 合成。

**B 档（半迁移，需用户自带端点/真机）**：Kokoro WASM 真机出声 · 视频生成（用户自带 fal/Seedance key）· 电商发布（需登录态/多平台）· 错误上报（用户自建端点）。

**C 档（不迁移，破坏单文件承诺）**：账号体系 · 微服务编排 · 云沙盒执行 · 图数据库知识图谱。

### 分批次授权建议（Phase B）

| 批次 | 内容 | 影响文件 | 风险 | 验证 |
|------|------|---------|------|------|
| A1 | 透明操作时间线（AuditEntry 字段集 + 导出） | demo.html + unit-test | 低 | 新增 T_OBS E2E |
| A2 | 多端点健康自愈池（冷却 TTL + 三态徽章） | demo.html + cfg 端点列表 | 中 | mock 502/429 重放 |
| A3 | 验收卡台账（VERDICT 协议 + 证据 ledger） | test-cases/manifest.json + verify-acceptance.cjs | 低 | 三向对齐门禁 |
| B1 | 本地记忆三层 + 画像页（IndexedDB 6 store） | demo.html + idb-util.cjs | 中 | 画像更新 e2e |
| B2 | 技能结晶（≥2 次→建议→确认成 skill） | demo.html + skill store | 中 | 结晶门控单测 |
| B3 | 场景向导框架（任务看板 + stop_at） | demo.html 新页签 | 中 | 看板状态机 e2e |
| B4 | PPT 向导（大纲→分页→几何审计→PptxGenJS） | demo.html + pptxgenjs CDN | 中 | 几何审计单测 |
| B5 | 电商文案向导（商品卡 + 草稿→预览→确认） | demo.html + 渠道规格表 | 低 | 商品卡 schema 校验 |
| C1 | 图片/视频场景扩展（复用 synthChain 状态机） | demo.html | 高 | 镜级失败重试 e2e |

**Next Gate**：等待用户批准批次范围后进入 Phase B。每批遵循"TDD RED→GREEN→回归 57/57 不回归 + 证据台账 + Critic 审查"。建议优先批准 **A1+A2+A3**（透明+自愈+台账，全部低风险、纯前端、直接消主项目三大痛点）。

| D-SKILL-1 | memorizz/ai-memory/gbrain/OpenKB/learn-agent/SkillOpt | ✅ | 技能结晶四维门控(executions≥5 ∧ 成功率≥0.80 ∧ 去重查询≥2 ∧ 30天内) + 排名公式 score=成功率×log1p(exec)×0.5^(天/14) + canonical hash 轨迹同一性 + SHADOW 影子验证(observations≥10 且 match_rate≥0.8 才 ACTIVE) + 用户纠正=gold signal + 蒸馏 6 道机器校验 + 学习中心两级加载(目录常驻/正文按需) + ≥5 决策规则+触发谓词式描述 |

> **真正的 22/22 完成确认（2026-09-07 23:30）**：D-SKILL-1 是最后一份报告。至此 Phase A' 全库深度扫描 22 个子代理全部回收，无遗漏、无空壳。
>
> **技能结晶最终算法（主项目 ≤80 行 JS 可落地）**：
> 1. `ops[]` 操作日志（action/参数形状/成败/ts/query）→ canonical hash 聚合（丢弃参数值，保留 (动作,参数键,是否出错) 有序序列，重试折叠）
> 2. 四维门控（≥5 次 ∧ ≥80% 成功 ∧ ≥2 种问法 ∧ 30 天内）→ 排名公式 → 弹"建议结晶 skill？"
> 3. 用户确认 → 存 skillbox；蒸馏调用复用 Hermes 黑盒，6 道本地校验（frontmatter 解析/工具名解析/未声明工具/字数/字面值泄漏/LLM-judge）
> 4. SHADOW 影子验证：未确认不注入，后台比对真实运行，observations≥10 且 match_rate≥0.8 才转 ACTIVE
> 5. 用中学=学习中心抽屉：12 个特性各配 1 分钟图文 + "你在哪里用过"触发说明 + 难度标签

---

## Phase B 实施台账（2026-09-08 更新）

### 批次 A1 — 透明操作时间线 ✅ DONE
- demo.html：pushTimeline/loadTimeline/renderTimeline/exportTimeline + 环形 200 条 localStorage + 事件信封 {id,ts,kind,msg,ok,ctx}
- 全链路埋点：system/probe/engine1/engine15/engine2/engine3；被拦与失败照样入库；写盘前脱敏
- 🧾 透明时间线 按钮 + 导出 JSON + 清空；面板展开时实时追加
- 验证：A1 面板+朗读事件 E2E（7 条目含引擎级）· 全量回归 17/17

### 批次 A2 — 多端点健康自愈池 ✅ DONE（上一轮提交 699dadd）
- unit-test-health.cjs A2_1-5 + demo.html healthPoolInit/probeAllEndpoints/cfg 备用端点/fetchTTSChunk 路由/dtype 兼容链

### 批次 A3 — 验收卡台账门禁 ✅ DONE
- verify-acceptance.cjs：E2E 断言数 + 单元断言数三向一致性，落盘 evidence/acceptance-ledger.ndjson
- evidence/manifest.json：A1/A2/A3 特征卡（expect + verifiedBy）
- 实测：E2E 29 + 单元 33 = 62 PASS

### 批次 A1-A3 后续（v4.3/v4.4）✅ DONE
- B1 本地记忆三层 + 画像卡（unit-test-memory.cjs B1_1-4）· B2 技能结晶 UI（四维门控建议 + 一键结晶）→ v4.4 落地，全量回归 66 PASS（E2E 29 + 单元 37）
- 已发版：v4.3.0/v4.3.1/v4.3.2/v4.4.0/v4.4.1/v4.4.2 + CI/CD 5 阶段 Pipeline + Pages 部署

### 批次 B3/B4/B5 — 场景向导 ✅ DONE（2026-09-09，commit a0cc5c3，tag v4.5.0）
- **B3 任务看板状态机**：demo.html `wzNewTask/wzCanTransit/wzGroupByStatus/wzRenderBoard/wzSeedBoard` + 五态看板（Backlog/Planning/Running/Review/Done）+ 取消列；新增→点击推进（合法流转门）→右键取消；localStorage `tts_wizard_v1` 持久化 + 时间线埋点
- **B4 PPT 大纲向导**：`wzParsePptSpec` 空行分页解析（标题+要点，页数/要点钳位）→ `wzDetectOverflow` 几何审计（中文宽度估算 + autofit 缩字 + 溢出标记）→ 分页预览 + 导出 Markdown
- **B5 电商文案向导**：`wzValidateCommodity` 商品卡 schema 校验 → `wzEcomTemplate` 三段草稿（标题版/正文版/快发版）→ `wzCensor` 禁用词替换（最低价/全网最低/第一/绝对/100%→★）→ 一键复制
- 测试：新增 unit-test-wizard.cjs（WZ_1-6）+ e2e-test-wizard.cjs（T_WZ_1-6）；verify-acceptance 门槛升级（E2E≥35 且 单元≥39）
- **全量回归（真实运行）：78/78 PASS**（35 E2E + 43 单元）；CI/CD 全绿（Build/Quality/Test 分片/Security/Deploy Pages+Release v12）；README v4.5 + workflow_status §10 + manifest.json 已同步；dist 已同步

### 待办（诚实披露）
- [ ] C1：图片/视频场景扩展（复用 wizard 状态机 + synthChain，v4.6）
- [ ] 引擎1.5 真机出声测（外部受限：需真机浏览器 + 82MB WASM 模型）
- [ ] CI action @v5 升级（Node 24 已自动跑，不影响功能）
- [ ] 变更报告-v6.html 已生成（本目录），发布前再核验一遍数字
